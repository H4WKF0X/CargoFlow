package backend.engine.tick;

/** Updates demand and spawns orders. */
public final class MarketSystem implements SimulationSystem {
    @Override public void update(TickContext ctx) { /* TODO */ }
}
